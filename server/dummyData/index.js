import requestStatuses from './requestStatus';
import requestTypes from './requestType';
import requests from './request';

const data = {
  requestStatuses,
  requestTypes,
  requests,
};

export default data;
